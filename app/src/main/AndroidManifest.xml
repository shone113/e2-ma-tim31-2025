<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitQuest">
        <activity
            android:name=".presentation.ui.ShopActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.AllUsersActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".presentation.ui.AllUsersActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".presentation.ui.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ui.TaskDetailsActivity" />
        <activity android:name=".presentation.ui.NewTaskActivity" />
        <activity android:name=".presentation.ui.TaskEditActivity" />
        <activity android:name=".presentation.ui.TaskCalendarActivity" />
        <activity android:name=".presentation.ui.TaskListActivity"/>
        <activity android:name=".presentation.ui.CategoryListActivity"/>
        <activity
            android:name=".presentation.ui.ProfileActivity"
            android:exported="false" />
    </application>

</manifest>