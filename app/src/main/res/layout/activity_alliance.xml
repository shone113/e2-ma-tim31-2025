<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.ui.AllianceActivity">

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilAllianceName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btnCreateAlliance"
        app:layout_constraintTop_toTopOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etAllianceName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Alliance name"
            android:inputType="textCapWords"
            android:maxLines="1"
            android:imeOptions="actionDone"
            tools:enabled="true" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- CREATE (initially visible) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCreateAlliance"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:layout_marginEnd="16dp"
        android:text="Create"
        android:tooltipText="Create alliance"
        android:contentDescription="Create alliance"
        app:cornerRadius="10dp"
        android:padding="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tilAllianceName"
        app:layout_constraintBottom_toBottomOf="@id/tilAllianceName" />

    <!-- DISBAND (initially hidden; appears after creating) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDisbandAlliance"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:layout_marginEnd="16dp"
        android:text="Disband"
        android:tooltipText="Disband alliance"
        android:contentDescription="Disband alliance"
        android:visibility="gone"
        app:cornerRadius="10dp"
        android:padding="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tilAllianceName"
        app:layout_constraintBottom_toBottomOf="@id/tilAllianceName"
        tools:visibility="gone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnChat"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:text="Open chat"
        app:icon="@drawable/chat"
        app:iconGravity="textStart"
        app:iconSize="30dp"
        app:cornerRadius="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tilAllianceName" />


    <ListView
        android:id="@+id/lvFriends"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:dividerHeight="8dp"
        app:layout_constraintTop_toBottomOf="@id/btnChat"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>