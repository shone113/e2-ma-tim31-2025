<resources>
    <style name="Theme.HabitQuest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Ovo boji pozadinu SVAKOG prozora -->
        <item name="android:windowBackground">@color/app_blue</item>

        <!-- Ako želiš i status/nav bar u istoj boji -->
        <item name="android:statusBarColor">@color/app_blue</item>
        <item name="android:navigationBarColor">@color/app_blue</item>
    </style>
</resources>
